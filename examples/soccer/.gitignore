# Model weights and sample videos downloaded by setup.sh / streamlit_app.py / gdown
data/
*.pt
*.mp4
*.avi
*.mov
*.mkv

# ...except the pre-rendered demo clips committed on purpose for the
# "Exemples pré-calculés" gallery in streamlit_app.py
!demo_data/**/*.mp4

# Local virtual environments some notebooks were run from
.venv/
venv/
myenv/
rf-env/

# Ultralytics training outputs and Roboflow dataset exports pulled in ad hoc
notebooks/runs/
notebooks/football-ball-detection-*/
notebooks/football-field-detection-*/
notebooks/output.txt
